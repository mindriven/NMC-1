pipeline {
    agent any 
    stages {
        stage('sample step 1') { 
            environment {
                scannerHome = tool 'SonarRunner 3.3.0.1492'
            }
            steps {
                withSonarQubeEnv('SQStandardSettings') {
                    sh "${scannerHome}/bin/sonar-scanner"
                }
            }
        }
        // stage('sample step 2') { 
        //     steps {
        //         // 
        //     }
        // }
        // stage('sample step 3') { 
        //     steps {
        //         // 
        //     }
        // }
    }
}